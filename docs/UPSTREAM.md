# Upstream tracking

The baseline is reviewed history, not a merge target. A read-only GitHub inventory on 2026-09-09 found
upstream `miuuyy/codex-chatgpt-web` `main` advanced to `e85e3693fdb4e3e033348c08df0298c20fcdb612` — three
commits ahead of the prior `c648c095` baseline: the squashed v5.0.5 and v5.0.6 release commits and the
merged [#402](https://github.com/miuuyy/codex-chatgpt-web/pull/402). The latest PR is open, non-draft
[#415](https://github.com/miuuyy/codex-chatgpt-web/pull/415) at
`826f8804f59cf8972571018cfb8b15b5166c3a8b`, the latest non-PR issue is
[#416](https://github.com/miuuyy/codex-chatgpt-web/issues/416), and the only upstream branch is `main`
at that SHA. The fork is **Windows-only**: reviews adopt only verified Windows-applicable work.

## Open pull-request decisions

| PR | Head | Decision | Windows applicability and reason | Reconsider when |
| --- | --- | --- | --- | --- |
| [#347](https://github.com/miuuyy/codex-chatgpt-web/pull/347) | `df0224756d639edfa2124ed96c1dad75e05e06df` | adopt | The new head extends the ported current-thread visualization-root rule to the authenticated parent thread; Windows regression coverage accepts only that shape and still rejects unrelated roots. | Codex changes the private visualization-root shape or parent-thread metadata. |
| [#343](https://github.com/miuuyy/codex-chatgpt-web/pull/343) | `96070ea6f8c4a181b624a0f626e174107b5874d4` | defer | Japanese launcher diagnostics are not a verified Windows defect and the PR has no checks. | It gains checks or a Windows-localized diagnostic regression. |
| [#340](https://github.com/miuuyy/codex-chatgpt-web/pull/340) | `ffef4f335c48b521562d4d21c26bb1c89e12f64e` | adopt | Fixes V1 null-agent-path lineage, linked to Windows issue #314; minimal fail-closed compatibility and tests are ported. | Codex changes V1 rollout metadata. |
| [#338](https://github.com/miuuyy/codex-chatgpt-web/pull/338) | `37dbecce686bb19fe8871699e205d43ca0dbe5f8` | defer | No committed regression tests or checks prove the Luna Think behavior. | A tested Windows reproduction and checks are supplied. |
| [#329](https://github.com/miuuyy/codex-chatgpt-web/pull/329) | `c55b5cef7f7a6d1b8c0759d7806a94f06d10ce77` | defer | Localized confirmation handling is potentially Windows-relevant, but the branch has a dirty merge conflict and no checks. | The conflict is resolved and focused tests pass. |

## 2026-09-08 follow-up: #360–#362

The PRs below are open, non-draft, and `UNSTABLE`. None of their branches was merged or fetched; #362's
minimal fix was independently ported and verified in this fork. Author-reported tests alone do not establish
this fork's Windows acceptance.

| PR | Exact observed state / head | Decision | Windows applicability and rationale | Re-review trigger |
| --- | --- | --- | --- | --- |
| [#360](https://github.com/miuuyy/codex-chatgpt-web/pull/360) | open, non-draft, `UNSTABLE`; `a4cd7012150bd76789fc818bd04d55cfa90a2744`; no upstream review or comments | defer | Its opt-in `external-provider` route mode is explicitly an unsupported local-fork patch. This fork has no concrete external-router requirement; its broad setup, launcher, configuration-journal, native-turn, and subagent ownership changes have no bounded Windows compatibility or security acceptance. | A concrete Windows external-router requirement exists; upstream stabilizes and accepts a reviewed implementation without an uncommitted handoff; and a bounded Windows compatibility/security acceptance proves the ownership transfer fails closed for config, journal, native turns, launcher/setup, and subagents. |
| [#361](https://github.com/miuuyy/codex-chatgpt-web/pull/361) | open, non-draft, `UNSTABLE`; `c09aa18b8a2a84e3fa3d77dcd4b339d3575cbbe6` | reject | The Dock/menu-bar-only preference and its prerequisites are explicitly macOS-only, including a fix for macOS issue #286; they are outside this Windows-only fork's supported scope. | The owner authorizes a separately bounded Windows product requirement with a Windows-applicable design and regression evidence. |
| [#362](https://github.com/miuuyy/codex-chatgpt-web/pull/362) | open, non-draft, `UNSTABLE`; `80ee0e3eac62067c14dd719d702ae1d7c55fdbe5` | adopt | Windows v5.0.4 Full-mode compaction can lose its continuation environment when Codex rebuilds one user preamble with sibling parts. The minimal port reads `<environment_context>` per array part, deduplicates identical claims, rejects distinct claims, and keeps bare strings off primary authority paths; continuation-only string claims remain cross-checked with native rollout authority. The three focused regressions pass. | The PR head changes, a Windows Full-mode continuation regression fails, or Codex changes content-part/rollout-authority shape. `contextualUserMessage` joined-text classification is deliberately excluded; it needs a separately bounded vocabulary/ordering review. |

## 2026-09-09 follow-up: #363–#416 complete register

This register covers every PR and issue number from #363 through #416 (54 numbers; 14 were never
allocated — #382–#393, #401, #409 all return 404) and the three-commit `main` advance from
`c648c09501bb1b704c7ad5273fb5f5d6b8992dd2` to `e85e3693fdb4e3e033348c08df0298c20fcdb612`. No upstream
write, fetch, merge, or release action was taken; every observed state below is what the GitHub API
returned on 2026-09-09. No code was ported this round: items this fork judged should eventually be
adopted are marked `defer` (never `adopt`, which this document reserves for changes already ported and
verified) and are called out explicitly, both in the Re-review trigger column and in the round's
implementation report to the primary session.

`main` moved by exactly three commits: the squashed v5.0.5 release (`0b053b6750b1d4f127619765388eb43c6d212ca2`,
41 files, +2019/-334), the squashed v5.0.6 release (`e85e3693fdb4e3e033348c08df0298c20fcdb612`, +783/-374),
and the individually reviewed merge of PR #402 (`509cfc97285cc6793af5c0e161153db0b405a767`, 3 files,
+36/-16, folded into the v5.0.6 commit). Both release commits are first-party squashes bundling many of
the individually tracked reports below (see #372, #376, #377, #379, #394, #397) plus unattributed
maintenance not tied to any single #363–#416 report. Given the size of the two release squashes, a full
unattributed line-by-line diff audit was out of scope for this pass; decisions rest on the traceable,
individually reviewed reports in the table below, two of which (#376, #415) this fork's own source and
`bun audit` output were checked against directly this round. No previously-reviewed row's re-review
trigger was concretely satisfied by this round's observations (the #395/#396 stale "Stopped thinking"
investigation is related to but does not itself satisfy #351's Windows-safe-log-trace trigger, since
neither PR supplies a live Windows safe-log export).

| Item | Exact observed state / head | Decision | Windows applicability and rationale | Re-review trigger |
| --- | --- | --- | --- | --- |
| [#363](https://github.com/miuuyy/codex-chatgpt-web/issues/363) | closed issue, `not_planned`, `bug` | reject | Maintainer confirmed this is intended behavior: closing the launcher window with **Keep running on close** keeps the bridge alive; fully quitting the process stops it by design. No Windows-specific defect shown. | A bounded Windows reproduction shows **Keep running on close** genuinely fails to keep the bridge alive. |
| [#364](https://github.com/miuuyy/codex-chatgpt-web/issues/364) | closed issue, `completed`, locked, `enhancement`/`P3`/`backlog`; converted to a GitHub Discussion, no comment recorded | defer | Retrying an oversized turn with progressively trimmed history is a general reliability idea, not Windows-specific, and was moved off the issue tracker into Discussions rather than accepted for implementation. | The retained Discussion produces a bounded design and a Windows-reproducible size-rejection retry case this fork can verify. |
| [#365](https://github.com/miuuyy/codex-chatgpt-web/pull/365) | closed, unmerged, draft; `c98cf139e185cc39d582c43b45aabd8603fde6a9` | reject | Maintainer closed this exact contribution as superseded by first-party v5.0.5 work tracked at issue #376; the underlying Windows extended-length/UNC rollout-path defect it targets is reviewed below at #376, where this fork's own source is confirmed still to lack the fix. | The PR is reopened or its head changes, or #376's adoption candidacy below is resolved. |
| [#366](https://github.com/miuuyy/codex-chatgpt-web/pull/366) | closed, unmerged; `6337889e8bc8ea33129f7edf931f35a9b82a9578` | reject | Maintainer kept the package-script portability change out of the runtime patch, preserved it at #378, and later confirmed both changes are already covered by the shipped v5.0.6 implementation — this fork's own `tools/dev_check.ps1`/`bun run verify` already pass, so no gap is established against this fork specifically. | This fork's own verify/typecheck scripts start failing on a packaged Bun runtime without a `bunx` shim, or PowerShell fails to expand a test-file glob. |
| [#367](https://github.com/miuuyy/codex-chatgpt-web/pull/367) | closed, unmerged; `e3e230deebb796079b1cc87be21dbe64cd64ba33` | reject | Maintainer preserved the injected-discovery-purity concern at #379 rather than take this implementation; #379 (below) is treated as the tracked, adoption-relevant successor. | #379's re-review trigger below fires. |
| [#368](https://github.com/miuuyy/codex-chatgpt-web/pull/368) | closed, unmerged; `7d63cc92ee884ef0a68674720adfdbecd86671a3` | reject | Superseded by first-party v5.0.5 work tracked at #377 (below); this specific contributor implementation was not taken. | #377's re-review trigger below fires. |
| [#369](https://github.com/miuuyy/codex-chatgpt-web/pull/369) | closed, unmerged; `a3c9c4ebbed77c6b7ef6cf47e6cb951f050e4f09` | reject | Maintainer found the proposed archived-rollout fallback did not sufficiently distinguish an archived record from other lookup failures or preserve every validation boundary; kept as a backlog design proposal at #380 (below), not implemented anywhere. | A stricter, boundary-preserving design (per the #380 follow-up comment) is implemented and reviewed. |
| [#370](https://github.com/miuuyy/codex-chatgpt-web/pull/370) | closed, unmerged; `5a7abe344a4bccdbe34dde649fb2f5d50d6d581a` | reject | Maintainer rechecked the cwd-less ambient-diff case against current native Codex requests and could not reproduce the reported failure with existing handling; declined a broader authority fallback without a matching failing trace. | A fresh native trace reproduces the failure against current `main` behavior. |
| [#371](https://github.com/miuuyy/codex-chatgpt-web/pull/371) | closed, unmerged; `a83a5472c8969f5c626e652c0a082e9af18d7d14` | reject | Maintainer judged the FIFO handoff did not sufficiently account for cancellation, cleanup, and retention lifecycle; kept existing behavior and pointed to #307/#308 for the underlying interrupted-task continuation case. | A handoff design proven safe across cancellation, cleanup, and retention is reviewed. |
| [#372](https://github.com/miuuyy/codex-chatgpt-web/issues/372) | closed issue, `completed`, `bug`/`P2`/`fix: released` | defer | Fixed in v5.0.5: both new and already-running tunnels must expose verified MCP diagnostics before monitoring starts; a missing diagnostics endpoint is no longer treated as readiness proof. Related to the still-monitored #354/#186. This fork's own tunnel-adoption/monitoring-start code was not independently source-checked this round. | Escalated as an adopt candidate pending source confirmation against this fork's tunnel-monitoring startup path. |
| [#373](https://github.com/miuuyy/codex-chatgpt-web/pull/373) | closed, unmerged; `29d9c2da3b57770ce49645f02319ffd580c4cfde` | reject | Maintainer requires evidence that a global page-driving lock both fixes the actual stalls and preserves cancellation/independent-turn progress; not established here. Preserved as an investigation at #381 (below). | #381's re-review trigger below fires with a controlled-workload comparison. |
| [#374](https://github.com/miuuyy/codex-chatgpt-web/issues/374) | open issue, `bug`/`enhancement`/`P2`/`investigation` | reject | Maintainer explicitly declined the proposed connector-naming/identity redesign after reviewing the investigation ("keeping fixed connector names... not planning the proposed naming/identity redesign"); the custom-name option was removed entirely in v5.0.6. Remains open only as an unresolved user complaint about the removal's effect on a multi-device setup, not an accepted design. | The owner authorizes a bounded, Windows-scoped connector-naming design distinct from the declined redesign. |
| [#375](https://github.com/miuuyy/codex-chatgpt-web/pull/375) | closed, unmerged; `9b62e1264d91f1f922c869d1d10501104ede8c0f` | reject | Maintainer already has local message-budget/multipart fixes for #346/#353 and judged this PR's separate fixed page-capacity policy not established by the supplied evidence; declined. | A page-capacity limit is established with evidence beyond this PR and reconciled with #346/#353. |
| [#376](https://github.com/miuuyy/codex-chatgpt-web/issues/376) | closed issue, `completed`, `bug`/`P2`/`Windows`/`fix: released` | defer | **Confirmed Windows defect, independently verified against this fork's own source.** Upstream's fix normalizes `\\?\`/UNC extended-length Windows rollout paths before containment/identity comparison. This fork's `pathIdentity()` — duplicated verbatim in `src/adapters/chatgpt-web/environment.ts:70-73`, `src/adapters/chatgpt-web/codex-rollout-environment.ts:43-46`, and `src/adapters/chatgpt-web/thread-environment.ts:44-47` — only `resolve()`s and lowercases on `win32`; it does not strip the `\\?\`/`\\?\UNC\` prefix, so it can still reject a legitimate namespaced rollout path exactly as #365/#376/#399 describe. Not ported this round per scope. | Escalated as an adopt candidate in this round's report; port `toNamespacedPath()`-equivalent normalization into all three `pathIdentity()` copies with a Windows regression test, then update this row to `adopt`. |
| [#377](https://github.com/miuuyy/codex-chatgpt-web/issues/377) | closed issue, `completed`, `bug`/`P2`/`fix: released` | defer | Upstream retires a Zero Risk tab's retained-conversation binding on navigation so a resumed turn can't reuse stale page ownership; shipped in v5.0.5. This fork's manual/Zero Risk navigation-invalidation path was not independently source-checked this round, so presence or absence in this fork is unconfirmed. | Escalated as an adopt candidate pending source confirmation; if this fork's browser-host navigation handling lacks the same invalidation, port it with a focused regression, then update this row. |
| [#378](https://github.com/miuuyy/codex-chatgpt-web/issues/378) | closed issue, `completed`, locked, `enhancement`/`P3`/`backlog` | reject | Same conclusion as #366: maintainer confirmed both package-script portability changes are already covered by the shipped v5.0.6 implementation; no gap established against this fork. | Same trigger as #366 above. |
| [#379](https://github.com/miuuyy/codex-chatgpt-web/issues/379) | closed issue, `completed`, `bug`/`P3`/`fix: released` | defer | Upstream keeps injected discovery deterministic when a synthetic test/dev environment is supplied, without consulting the live Windows registry; shipped in v5.0.6. This is a dev/test-path purity fix with no live-user product impact; not independently source-checked this round. | Escalated as a low-priority adopt candidate; confirm whether this fork's `installedLauncherCandidates()`-equivalent has the same impurity before porting. |
| [#380](https://github.com/miuuyy/codex-chatgpt-web/issues/380) | closed issue, `completed`, locked, `enhancement`/`P2`/`backlog` | defer | Maintainer agreed the original #369 fallback was too broad and sketched a stricter design (valid / explicitly-archived-and-matching-owner / invalid) in the follow-up comment, but no implementation exists anywhere yet. | The stricter design is implemented and reviewed upstream, or the owner authorizes this fork to design it independently. |
| [#381](https://github.com/miuuyy/codex-chatgpt-web/issues/381) | closed issue, `completed`, locked, `enhancement`/`P2`/`backlog` | defer | Global page-driving serialization (#373) was declined as premature; a narrower measurement-only instrumentation proposal exists only as a follow-up comment, not an implementation. Related to this round's #397 finding (below), which independently confirms a real page-acquisition contention defect without a lock. | A measurement-only diagnostic patch or #397's shipped fix is reviewed against this fork. |
| [#394](https://github.com/miuuyy/codex-chatgpt-web/issues/394) | closed issue, `completed`, `bug`/`P1`/`fix: released`/`investigation`/`awaiting retest`; reporter confirmed resolution in a follow-up comment | defer | Upstream added explicit Setup recovery for a fully removed managed interrupt hook in v5.0.6. This fork's `src/codex-interrupt-hook.ts` throws the identical `"...refusing to overwrite it"` error family (verified by direct read), and no separate recovery path was found elsewhere in `src/`. Plausibly the same unrecovered failure mode in this fork, though the launcher-side Setup wiring was not exhaustively audited this round. | Escalated as an adopt candidate; audit `launcher:setup-core` handling for a removed hook and, if the harsh failure persists, port a bounded recovery path with a regression test. |
| [#395](https://github.com/miuuyy/codex-chatgpt-web/pull/395) | closed, unmerged; `9be751b3274a2aca43453608d4d9d666e6641737` | reject | Maintainer found the 10-minute suppression policy still produces false cancellations after the window elapses, and an unreadable Stop-button read still degraded to "not running"; declined this exact change and implemented a different first-party policy (surfacing ChatGPT's own status directly, with no added waiting window) instead. Not confirmed as a Windows safe-log trace, so this does not concretely satisfy #351's existing re-review trigger. | A Windows safe-log trace of a stale-"Stopped thinking" cancellation is captured against current `main` behavior. |
| [#396](https://github.com/miuuyy/codex-chatgpt-web/pull/396) | closed, unmerged; `6a52fd00aed7c28bc74d3b48cca93ba528580afa` | reject | Maintainer found the deferral-notice probe only fires on an empty response while the reported failure had a nonempty one, and the page-wide text lookup could false-positive on a quoted notice; declined as an unconfirmed hypothesis. Also inherited #395's declined suppression policy. | The deferral notice is confirmed on-screen during an actual failing turn with a privacy-safe capture. |
| [#397](https://github.com/miuuyy/codex-chatgpt-web/issues/397) | **open**, `bug`/`P1`/`fix: released`/`investigation`/`awaiting retest` | defer | Upstream's v5.0.6 removes page-acquisition dependency on unrelated renderer targets, so a paused peer page no longer blocks a responsive page owned by the current task; inherently Windows-relevant since this fork only runs on Windows. Issue remains open pending the reporter's retest, and this fork's own browser-page-acquisition code was not independently source-checked this round. | Escalated as an adopt candidate; confirm this fork's page-selection logic against native browser target identity, and re-check once upstream closes #397 after retest. |
| [#398](https://github.com/miuuyy/codex-chatgpt-web/issues/398) | closed issue, `completed`, `bug`/`awaiting retest`; reporter confirmed resolution | monitor | The post-compaction `<environment_context>` claim handling this addresses overlaps with this fork's already-adopted PR #362 (2026-09-08 follow-up section). Recommend confirming existing coverage rather than treating this as a fresh, unaddressed gap. | This fork reproduces the same `Compaction continuation requires one current native environment claim` failure despite the #362 port. |
| [#399](https://github.com/miuuyy/codex-chatgpt-web/pull/399) | closed, unmerged, `dirty`; `31ba066893b8f7b3d64ec3e135f05db7f3dedbe5` | reject | The PR author's own follow-up comment confirms v5.0.5 already fixes both targeted defects through a *different* implementation than this PR's; closed as superseded. That different upstream implementation is exactly the #376 gap above — this fork has neither implementation, so #376 carries the adoption candidacy, not this PR. | Same trigger as #376 above. |
| [#400](https://github.com/miuuyy/codex-chatgpt-web/pull/400) | closed, unmerged; `0d389e9837e8df6f4b81ac5e326cc9f2e75bc0c5` | reject | Maintainer is consolidating this into a combined Japanese/Chinese diagnostics-localization branch and closed this standalone PR; the idea is tracked in a Discussion (converted from #358, itself already `reject` in this fork's register) rather than an accepted implementation. | A combined localization implementation ships in a release and the owner authorizes non-English launcher-diagnostics support for this Windows-only fork. |
| [#402](https://github.com/miuuyy/codex-chatgpt-web/pull/402) | closed, **merged** 2026-09-08T09:38:33Z into upstream `main` (`509cfc97285cc6793af5c0e161153db0b405a767`, folded into the v5.0.6 commit); head `f69ec84c7b9c9fc8c21c4be4d67538e43ca3afdf` | defer | Recognizes Simplified Chinese Temporary Chat personalization labels (`个性化`/`非个性化`) in the existing labeled preflight path; bounded (3 files, +36/-16), ships parameterized English/Chinese regression tests, and is browser-automation locale-detection logic with no macOS/Linux-specific dependency. Already reviewed and merged upstream, which lowers risk relative to an unreviewed contribution, but this fork's own `src/adapters/chatgpt-web/browser-worker.ts` was not independently diffed against it this round. | Escalated as an adopt candidate; diff this fork's personalization preflight against the merged commit and port with the included regression tests. |
| [#403](https://github.com/miuuyy/codex-chatgpt-web/pull/403) | closed, unmerged, `dirty`; `d7249d5a8a4f23ac044120a595d5517113829561` | reject | Maintainer declined: broadens process/profile handling, weakens login verification, and leaves helper-packaging gaps not justified by a confirmed current failure. The cited Bun extra-pipe bug (oven-sh/bun#27977, fixed by oven-sh/bun#31829) is already an ancestor of this fork's pinned Bun 1.4.0. Preserved for reproduction at #405 (below). | #405's re-review trigger below fires with a reproducible failure on the pinned Bun 1.4.0. |
| [#404](https://github.com/miuuyy/codex-chatgpt-web/pull/404) | closed, unmerged; `451b8a372094c8594e3a9c6cfb7b7ab1de0f1394` | monitor | Maintainer confirms the missing `collaboration.wait_agent` 10-second guard is a real gap and has a passing local fix (covering direct calls, both exec paths, and a real installed-Codex/MCP check) tied to existing transport work (#276), but it is **not yet released**. Nothing shippable to port yet. | The fix ships in a release; then treat as a fresh adoption candidate against this fork's collaboration-wait guard. |
| [#405](https://github.com/miuuyy/codex-chatgpt-web/issues/405) | open issue, no labels; maintainer's own analysis, no comments | monitor | Maintainer opened this to track reproduction of the standalone Windows CLI login timeout independently of the declined #403 implementation; explicitly requests exact Bun/Windows/Chrome versions and a minimal trace on the pinned runtime, none of which has been supplied yet. No fix proposed or shipped. | The requested reproduction evidence (or this fork's own login timeout on Bun 1.4.0) appears. |
| [#406](https://github.com/miuuyy/codex-chatgpt-web/issues/406) | open issue, `bug` | reject | Maintainer determined this is a mode misunderstanding (Browser-only deliberately has no tools; Full harness does) compounded by an outdated v5.0.0 install, not a Plus-account or product defect; asked for a v5.0.6 retest with a safe-log export if it persists. | The reporter retests on v5.0.6 in Full harness mode and the failure persists with a safe-log export. |
| [#407](https://github.com/miuuyy/codex-chatgpt-web/issues/407) | open issue, no labels | monitor | Maintainer requested a privacy-safe **Export safe log**; reporter has so far supplied only raw, unredacted diagnostics (explicitly flagged as containing private paths) rather than the requested safe export, so the underlying cause (capacity message vs. an unrelated bridge failure; `turn_id` metadata conflict on reused threads) remains unestablished. | A privacy-safe safe-log export is supplied and correlates the two error timestamps to a specific launcher-side cause. |
| [#408](https://github.com/miuuyy/codex-chatgpt-web/issues/408) | open issue, no labels, no maintainer triage yet | monitor | "No effective Codex Native tool access" on every goal execution is a plausible real Windows defect (reported against v5.0.6), but there is no maintainer triage, no isolated reproduction, and no safe-log export yet. | Maintainer triage or a privacy-safe reproduction narrows the cause. |
| [#410](https://github.com/miuuyy/codex-chatgpt-web/issues/410) | open issue, `bug` | reject | Reported Platform is explicitly **macOS arm64**; outside this Windows-only fork's supported scope. The report body also carries no diagnostic content beyond repeating the title in every field. | It becomes Windows-reproducible with an actual diagnostic report. |
| [#411](https://github.com/miuuyy/codex-chatgpt-web/issues/411) | open issue, no labels | defer | Documents a real gap: the bug-report form's required Integration-mode dropdown has no explicit Zero Risk option, and TROUBLESHOOTING.md has no Zero Risk section. Directly addressed by open PRs #412 and #413 (below), which are docs-only and bounded. | #412/#413's re-review trigger below fires. |
| [#412](https://github.com/miuuyy/codex-chatgpt-web/pull/412) | **open**, non-draft, `unstable`; `e73e910bdf2fd36bf7e0cde92d27930b26660440`; fixes #411 | defer | Bounded, docs/issue-form-only change: adds a `Zero Risk (manual handoff)` mode option and troubleshooting guidance; no runtime, routing, credential, or approval change. `unstable` mergeable state traces to the pre-existing root `bun audit` hono advisories (the same ones this fork independently reproduced at #415, not caused by this PR's diff). Not yet ported into this fork's own `.github/ISSUE_TEMPLATE`/`TROUBLESHOOTING.md`. | Escalated as a low-risk adopt candidate; port the issue-form and troubleshooting wording into this fork's own docs. |
| [#413](https://github.com/miuuyy/codex-chatgpt-web/pull/413) | **open**, non-draft, `unstable`; `210174707702bc64f872eff080c526295e5b5efb` | defer | Bounded, docs-only `README.zh-CN.md` sync adding the missing Zero Risk row/description already present in the English `README.md`. Same `unstable` root cause as #412 (pre-existing hono advisories, unrelated to this PR's diff). Not yet ported. | Escalated as a low-risk adopt candidate alongside #412. |
| [#414](https://github.com/miuuyy/codex-chatgpt-web/issues/414) | open issue, `bug`, very fresh (2026-09-09) | monitor | Windows x64 install/connection failure reported in Chinese with minimal actionable detail beyond a screenshot and a diagnostics export reference; no maintainer triage yet. | Maintainer triage or a translated, itemized reproduction narrows the cause. |
| [#415](https://github.com/miuuyy/codex-chatgpt-web/pull/415) | **open**, non-draft, `unstable`; `826f8804f59cf8972571018cfb8b15b5166c3a8b` (latest PR in the range) | defer | **Independently reproduced in this fork's own workspaces.** `bun audit` on the repository root reports the same 3 moderate `hono@4.12.34` advisories (GHSA-gqvv-2mrq-wpjv, GHSA-g6gw-c38x-mqfc, GHSA-crvj-82cr-hjcx, all fixed at ≥4.13.5) the PR describes, and `bun audit` in `launcher/` reports the same high `js-yaml@4.3.1` advisory (GHSA-2883-xcg3-v3hh, fixed at ≥4.3.2). Both are exact-version-bound dependency deferrals per `AGENTS.md`; a newer release fixing them has now resurfaced. `unstable` traces to the same pre-existing advisories, not to the PR's diff, which is limited to `package.json`/`bun.lock`/`launcher/bun.lock`. | Escalated as the highest-confidence adopt candidate; bump `hono` to ≥4.13.5 and `launcher`'s `js-yaml` to ≥4.3.2, with a passing `bun audit` on both workspaces. |
| [#416](https://github.com/miuuyy/codex-chatgpt-web/issues/416) | open issue, no labels, very fresh (2026-09-09) | defer | Reports Extra High becoming unavailable whenever Pro is unavailable despite a correctly detected four-step slider, and includes a complete local source patch plus regression tests (92 passed) and live-install verification against v5.0.6. Per this fork's standing rule, author-reported tests alone do not establish this fork's Windows acceptance, and upstream has not reviewed or merged it. | This fork independently reproduces the Extra-High/Pro coupling defect on its own installed v5.0.6-equivalent build and verifies the patch with its own regression run, or upstream reviews and accepts a version of it. |

## 2026-09-07 follow-up: #349–#359 complete register

The following live states were read from GitHub before moving the baseline. PR author claims and local
tests are not adoption evidence. Closed/unmerged and draft PRs remain unmerged history; no upstream
write, fetch, merge, or release action was taken.

| Item | Exact observed state / head | Decision | Windows applicability and rationale | Re-review trigger |
| --- | --- | --- | --- | --- |
| [#349](https://github.com/miuuyy/codex-chatgpt-web/pull/349) | closed, unmerged; `f9619efd69bf397fec19edcdec53156762a88c92` | reject | Upstream explicitly declined its 502 `Stopped thinking` taxonomy: that visible state does not distinguish manual Stop from an upstream interruption. Its tests therefore do not establish a Windows fix. | A privacy-safe Windows trace distinguishes the termination source, and a focused no-replay regression proves the resulting classification. |
| [#350](https://github.com/miuuyy/codex-chatgpt-web/issues/350) | closed issue; macOS arm64 setup report | reject | macOS-only connector-creation report is outside public Windows support. | A current Windows reproduction with a bounded local cause. |
| [#351](https://github.com/miuuyy/codex-chatgpt-web/issues/351) | open issue; macOS arm64 reports | reject | The generic stopped-response reports have no Windows reproduction and do not establish a common cause. | A privacy-safe Windows safe-log trace identifies the post-submit failure state without replaying the accepted turn. |
| [#352](https://github.com/miuuyy/codex-chatgpt-web/issues/352) | open issue; no PR | defer | A symlinked `config.toml` can matter on Windows, but only macOS isolated checkpoint evidence exists; a `stat`-only change could replace the link during rollback. | A Windows symlink/junction reproduction plus capture, setup, failure rollback, and removal tests prove both link and target bytes remain intact. |
| [#353](https://github.com/miuuyy/codex-chatgpt-web/issues/353) | open issue; no PR | defer | The report combines a legacy non-`chatgpt-web/*` compact passthrough 404 with a separate non-Pro Web staging-size problem. No bounded Windows fix is supplied. | A Windows regression isolates the legacy fallback and proves an actionable local error or safe local route without bundling a Web size-policy redesign. |
| [#354](https://github.com/miuuyy/codex-chatgpt-web/pull/354) | open draft; `466d682f0a054ed2699c65e86465e81815c141ce` | defer | Uncached loopback-health discovery could help Windows monitoring, but the draft has no Windows/package acceptance and changes deleted Japanese/Simplified README surfaces. | A non-draft, Windows-packaged reproduction proves inventory false-green behavior; then port only the smallest supervisor behavior and regression, retaining Windows-only docs. |
| [#355](https://github.com/miuuyy/codex-chatgpt-web/pull/355) | closed, unmerged draft; `1108fa6298fab765dce6a052b7edc2510c0bad17` | monitor | Upstream declined classification/retry policy because it leaves the viewport geometry/lifecycle cause unaddressed and has no installed Windows acceptance. The no-replay boundary remains valuable. | A Windows trace identifies the failed initial-acquisition or post-submit rebind transition, with an installed-path regression that preserves cancellation and never replays an accepted turn. |
| [#356](https://github.com/miuuyy/codex-chatgpt-web/issues/356) | open issue; no PR | monitor | Claimed Chinese per-call approval labels were not reproduced; contributor tests invented labels and cannot establish the rendered card. | A supported Windows per-call flow (auto-approval off) supplies privacy-safe observed labels/DOM and a regression that keeps one-time and persistent approval distinct. |
| [#357](https://github.com/miuuyy/codex-chatgpt-web/issues/357) | open issue; no PR | defer | It is a Windows Go/Think Full-mode report, but declined PR #338 bypassed ordinary Think enforcement and lacks fresh-versus-retained connector proof. | Reproduce fresh and retained Go/Full Think requests with connector retention and ordinary Luna controls; a candidate must retain explicit model/effort enforcement. |
| [#358](https://github.com/miuuyy/codex-chatgpt-web/issues/358) | open enhancement; no PR | reject | Japanese launcher-diagnostic localization is not a Windows-only public-support commitment; the prior formatter corrupts replacement tokens and changed non-Japanese output. | Owner authorizes Japanese UI support and a bounded implementation preserves dynamic values literally with token, quote, and backslash regressions. |
| [#359](https://github.com/miuuyy/codex-chatgpt-web/issues/359) | open enhancement; no PR | monitor | This is the retained #355 policy idea, not evidence that the viewport defect is fixed. Initial acquisition and post-submit rebind must stay distinct. | The geometry/lifecycle failure is reproduced on Windows and a focused fix proves cancellation plus no replay after `send_activated` and `accepted`. |

## Previously reviewed issue decisions

| Issue | Decision | Windows applicability and reason | Reconsider when |
| --- | --- | --- | --- |
| [#348](https://github.com/miuuyy/codex-chatgpt-web/issues/348) | monitor | Closed Windows 10 Full Harness report: TOML rewrite places the interrupt-hook end marker before its start marker; no PR or verified fix exists. | Upstream provides a regression and safe marker-normalization fix. |
| [#346](https://github.com/miuuyy/codex-chatgpt-web/issues/346) | monitor | Windows/Plus/Full report has no verified new fix. | A reproducible fix or validated regression test appears. |
| [#345](https://github.com/miuuyy/codex-chatgpt-web/issues/345) | defer | Windows/WSL feature requires a security design for path, runtime, and ownership boundaries. | A bounded design and Windows/WSL tests are reviewed. |
| [#344](https://github.com/miuuyy/codex-chatgpt-web/issues/344) | defer | Windows launcher from WSL needs the same explicit cross-environment security design. | A safe design and integration tests exist. |
| [#339](https://github.com/miuuyy/codex-chatgpt-web/issues/339) | reject | macOS-only investigation is outside this fork. | The report becomes Windows-reproducible. |
| [#332](https://github.com/miuuyy/codex-chatgpt-web/issues/332) | defer | Usage-estimate enhancement is not required for Windows-only maintenance. | A verified Windows product need and bounded design appear. |
| [#328](https://github.com/miuuyy/codex-chatgpt-web/issues/328) | monitor | Connector 404 investigation lacks logs and a verified Windows fix. | Privacy-safe evidence and a regression are supplied. |
| [#326](https://github.com/miuuyy/codex-chatgpt-web/issues/326) | reject | Cloudflare Tunnel companion is a new, out-of-scope product surface. | Owner authorizes a separate design. |
| [#323](https://github.com/miuuyy/codex-chatgpt-web/issues/323) | reject | macOS-only Luna stream report is outside this fork. | It becomes Windows-reproducible. |
| [#321](https://github.com/miuuyy/codex-chatgpt-web/issues/321) | monitor | Windows compaction report awaits retest and has no new fix. | Retest fails with safe evidence or a tested fix appears. |
| [#319](https://github.com/miuuyy/codex-chatgpt-web/issues/319) | reject | macOS arm64 report is outside this Windows-only fork. | It becomes Windows-reproducible with a bounded Windows fix. |
| [#314](https://github.com/miuuyy/codex-chatgpt-web/issues/314) | adopt-linked | Windows lineage failure is addressed by adopted PR #340 (`ffef4f335c48b521562d4d21c26bb1c89e12f64e`). | A post-port Windows regression fails. |
| [#312](https://github.com/miuuyy/codex-chatgpt-web/issues/312) | defer | Account-safety investigation needs verified evidence, not speculative automation changes. | A bounded, privacy-safe mitigation is tested. |
| [#308](https://github.com/miuuyy/codex-chatgpt-web/issues/308) | monitor | Windows search freeze awaits retest; no verified new fix exists. | Retest or a regression identifies a Windows fix. |
| [#297](https://github.com/miuuyy/codex-chatgpt-web/issues/297) | defer | Native V2 encrypted delegation is a broad protocol enhancement, not required by current V1 support. | Owner requests V2 work with a tested contract. |
| [#286](https://github.com/miuuyy/codex-chatgpt-web/issues/286) | reject | macOS Dock behavior is outside this fork. | It gains a Windows impact. |
| [#278](https://github.com/miuuyy/codex-chatgpt-web/issues/278) | monitor | Windows hidden-viewport report awaits retest and has no verified new fix. | Retest fails or a focused Windows regression is available. |

```text
GitHub read-only inventory
  ├─ main commit
  ├─ latest pull request
  ├─ latest non-PR issue
  └─ complete branch set
          │
          └─ all current -> pass; unavailable or changed -> attention/fail closed
```

Run `bun run check:upstream` before a maintenance delivery. The checker uses read-only GitHub API
queries; it never fetches, merges, pushes, or writes upstream. `--strict` exits non-zero for changed
or unavailable axes. When it reports attention, review the exact upstream change, record an
adopt/defer/reject decision here and in [DECISIONS.md](DECISIONS.md), then update the baseline only
after relevant validation.
